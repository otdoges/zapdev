# =============================================================================
# ZapDev Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env files to version control!

# =============================================================================
# REQUIRED - Core Application
# =============================================================================

# Convex Database URL
# Get this from your Convex dashboard: https://dashboard.convex.dev
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# WorkOS Authentication (Client ID - required for both server and client)
# Get from WorkOS Dashboard: https://dashboard.workos.com
VITE_WORKOS_CLIENT_ID=client_01234567890abcdef

# WorkOS Authentication (Server-side - same as client ID)
WORKOS_CLIENT_ID=client_01234567890abcdef

# WorkOS API Key (Server-side only)
# Get from WorkOS Dashboard  API Keys
WORKOS_API_KEY=sk_01234567890abcdef

# =============================================================================
# WORKOS SSO FLOW - Choose ONE of the following
# =============================================================================

# Option A: Domain-based Flow (Multi-tenant)
# Use this for multiple organizations with different identity providers
# Set up domains in WorkOS Dashboard  Single Sign-On  Domains
VITE_WORKOS_DOMAIN=acme.com

# Option B: Connection-based Flow (Single IdP)  
# Use this for a single identity provider for all users
# Get Connection ID from WorkOS Dashboard  Single Sign-On  Connections
# VITE_WORKOS_CONNECTION_ID=conn_01234567890abcdef

# WorkOS Redirect URI (optional - has fallback)
# Must match exactly what's configured in WorkOS Dashboard
VITE_WORKOS_REDIRECT_URI=http://localhost:5173/auth/callback

# =============================================================================
# AI SERVICES
# =============================================================================

# Groq AI API Key
# Get from: https://console.groq.com/keys
VITE_GROQ_API_KEY=gsk_1234567890abcdef

# =============================================================================
# ANALYTICS (Optional)
# =============================================================================

# PostHog Analytics
# Get from PostHog Dashboard: https://app.posthog.com
VITE_PUBLIC_POSTHOG_KEY=phc_1234567890abcdef
VITE_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# =============================================================================
# BILLING (Optional - for Polar integration)
# =============================================================================

# Polar Billing Integration
# Get from Polar Dashboard: https://polar.sh
VITE_POLAR_ACCESS_TOKEN=polar_at_1234567890abcdef
VITE_POLAR_ORGANIZATION_ID=org_1234567890abcdef

# Application URL (for billing callbacks)
# Use your actual domain in production
VITE_APP_URL=http://localhost:5173

# =============================================================================
# LEGACY (Supabase - Remove after migration complete)
# =============================================================================

# Legacy Supabase Configuration (can be removed after WorkOS migration)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# PRODUCTION ENVIRONMENT NOTES
# =============================================================================
# For production deployment, make sure to:
# 1. Use HTTPS for all URLs (especially VITE_WORKOS_REDIRECT_URI and VITE_APP_URL)
# 2. Set VITE_APP_URL to your actual domain
# 3. Configure CORS and redirect URIs in respective service dashboards
# 4. Use environment-specific values for each deployment stage
